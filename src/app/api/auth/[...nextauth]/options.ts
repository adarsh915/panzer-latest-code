// NextAuth is not used in this project.
// All credentials are stored in the `users` MySQL table (bcrypt-hashed passwords).
// Authentication is handled by /api/auth/login and verified via admin_sessions table.
export {}
