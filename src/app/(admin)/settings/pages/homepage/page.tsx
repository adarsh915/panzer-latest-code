import type { Metadata } from 'next'
import HomepageSettingsPanel from './HomepageSettingsPanel'

export const metadata: Metadata = { title: 'Homepage Settings' }

const HomepageSettingsPage = () => <HomepageSettingsPanel />

export default HomepageSettingsPage
